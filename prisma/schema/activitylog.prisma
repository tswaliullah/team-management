model ActivityLog {
  id          String   @id @default(cuid())
  action      String
  taskTitle   String
  fromMember  String?
  toMember    String?
  timestamp   DateTime @default(now())

  @@index([timestamp])
}
